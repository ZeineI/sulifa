package server

import (
	"github.com/gin-gonic/gin"
)

func (sv *Server) createRoom(c *gin.Context) {
	//check if player authrized
	//create room
	//create first player
	//add info to db
	//return room id
}

func (sv *Server) joinRoom(c *gin.Context) {
}
